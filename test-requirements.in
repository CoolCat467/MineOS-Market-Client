# For tests
pytest >= 5.0
coverage >= 7.2.5
pytest-trio
pytest-cov

# Tools
black; implementation_name == "cpython"
mypy >= 1.14.0  # Would use mypy[faster-cache], but orjson has build issues on pypy
orjson; implementation_name == "cpython"
ruff >= 0.6.6
uv >= 0.2.24
codespell

# https://github.com/python-trio/trio/pull/654#issuecomment-420518745
mypy-extensions
typing-extensions

# MineOS-Market-Client's own dependencies
#<TOML_DEPENDENCIES>
httpx~=0.28.1
trio~=0.28.0
#</TOML_DEPENDENCIES>
